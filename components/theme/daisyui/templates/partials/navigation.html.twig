<ul class="menu menu-horizontal px-1">
    {% for p in pages.children.visible %}
        {% set active = (p.active or p.activeChild) ? 'active' : '' %}
        {% if config.theme.dropdown.enabled and p.children.visible.count > 0 %}
            <li>
                <details>
                    <summary class="{{ active }}">{{ p.menu|e }}</summary>
                    <ul class="bg-base-100 rounded-t-none p-2 z-10">
                        <li><a href="{{ p.url|e }}">{{ p.menu|e }}</a></li>
                        {% for child in p.children.visible %}
                            <li><a href="{{ child.url|e }}">{{ child.menu|e }}</a></li>
                        {% endfor %}
                    </ul>
                </details>
            </li>
        {% else %}
            <li><a class="{{ active }}" href="{{ p.url|e }}">{{ p.menu|e }}</a></li>
        {% endif %}
    {% endfor %}
    {% for mitem in site.menu %}
        <li><a href="{{ mitem.url|e }}">{{ mitem.text|e }}</a></li>
    {% endfor %}
</ul>
